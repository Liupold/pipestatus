#!/bin/sh
echo "PLAYERCTL:"
playerctl -p cmus,%any -i chromium -F metadata -f 'PLAYERCTL:{{status}}: {{title}} ({{artist}})' &

playerctl -F status | while read -r status; do
        [ "$status" = 'Playing' ] && \
        playerctl metadata -f 'PLAYERCTL:{{status}}: {{title}} ({{artist}})'
done
